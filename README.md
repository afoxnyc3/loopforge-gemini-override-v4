# gemini-override-v4

Build a command-line bookmark manager in Python that stores URLs with tags in a SQLite database. Support add, search by tag, list, delete operations. Include import/export from browser bookmark HTML files.

Built by [LoopForge](https://github.com/partnerandagent/loopforge) — autonomous multi-agent development.
